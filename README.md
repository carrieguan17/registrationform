# registrationform

## To start the app, please perform the following steps:
1. npm install;
2. npm run react-dev;
3. npm start;
4. sqlite3 ./registration.db
5. (inside sqlite3 shell) .read schema.sql
